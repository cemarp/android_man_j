
<<<<<<< SEARCH
                // Draw solid walls for `walls` mode
                walls.forEach { wallPoints ->
                    if (wallPoints.size >= 4) {
                        // A wall captured using the 4-corner trace mode
                        // We can draw a polygon between its mapped coordinates
                    }
                }
=======
                // Draw solid walls for `walls` mode
                walls.forEach { wallPoints ->
                    if (wallPoints.size >= 4) {
                        // A wall captured using the 4-corner trace mode
                        // We can draw a polygon between its mapped coordinates
                        val path = Path().apply {
                            wallPoints.forEachIndexed { index, p ->
                                // Project straight up slightly depending on y
                                val heightProjectionFactor = 150f
                                val projectedZOffset = p.y * heightProjectionFactor

                                val nx = size.width / 2f + ((p.x - minX) * baseScale + padding - (maxX - minX) * baseScale / 2)
                                val nz = size.height / 2f - ((p.z - minZ) * baseScale + padding - (maxZ - minZ) * baseScale / 2) - projectedZOffset

                                if (index == 0) moveTo(nx, nz) else lineTo(nx, nz)
                            }
                            close()
                        }
                        drawPath(path = path, color = Color(0xFFFF69B4).copy(alpha = 0.4f))
                        drawPath(path = path, color = Color(0xFFFF1493), style = Stroke(width = 2f))
                    }
                }
>>>>>>> REPLACE
